const u= requireAuth("student");if(u){const s=getData(DB_KEYS.students).find(x=>x.id===u.id);let subjects=getData(DB_KEYS.subjects);let enrollments=getData(DB_KEYS.enrollments);function render(){const enrolled=enrollments.filter(e=>e.studentId===s.id).map(e=>e.subjectId);const q=(document.getElementById("subjectSearch")?.value||"").toLowerCase();const filtered=subjects.filter(x=>`${x.code} ${x.name} ${x.instructor}`.toLowerCase().includes(q));renderApp2("Available Subjects",`<div class="page-head"><div><span class="eyebrow">ACADEMICS</span><h1>Available Subjects</h1><p>Browse and enroll in subjects for your current semester.</p></div></div><div class="panel"><div class="toolbar"><input id="subjectSearch" placeholder="Search code, subject or instructor…" value="${q}"><select id="unitFilter"><option value="">All units</option><option value="2">2 units</option><option value="3">3 units</option></select></div><div class="table-wrap"><table><thead><tr><th>Code</th><th>Subject</th><th>Instructor</th><th>Units</th><th>Schedule</th><th>Slots</th><th>Action</th></tr></thead><tbody>${filtered.map(x=>`<tr><td><strong>${x.code}</strong></td><td>${x.name}</td><td>${x.instructor}</td><td>${x.units}</td><td>${x.schedule}</td><td>${x.slots}</td><td>${enrolled.includes(x.id)?'<span class="badge success">Enrolled</span>':`<button class="btn btn-primary btn-sm" onclick="enrollSubject('${x.id}')">Enroll</button>`}</td></tr>`).join("")||`<tr><td colspan="7" class="empty">No subjects found.</td></tr>`}</tbody></table></div></div>`);document.getElementById("subjectSearch").oninput=render;document.getElementById("unitFilter").onchange=()=>{const val=document.getElementById("unitFilter").value;if(val)subjects= getData(DB_KEYS.subjects).filter(x=>String(x.units)===val);else subjects=getData(DB_KEYS.subjects);render()}}window.enrollSubject=id=>{const current=getData(DB_KEYS.enrollments);if(current.some(e=>e.studentId===s.id&&e.subjectId===id))return toast("You are already enrolled.","error");const sub=getData(DB_KEYS.subjects).find(x=>x.id===id);if(!sub||sub.slots<=0)return toast("No available slots.","error");current.push({id:"ENR-"+Date.now(),studentId:s.id,subjectId:id,date:new Date().toISOString().slice(0,10)});saveData(DB_KEYS.enrollments,current);sub.slots--;saveData(DB_KEYS.subjects,getData(DB_KEYS.subjects).map(x=>x.id===id?sub:x));toast(`${sub.name} enrolled successfully.`,"success");render()};render()}function renderApp2(title,content){const u=currentUser();document.getElementById("app").innerHTML=`<div class="app-layout"><aside class="sidebar" id="sidebar"><a class="brand" href="dashboard.html"><span class="brand-mark">U</span><span><strong>University</strong><small>Enrollment Portal</small></span></a><nav class="side-nav"><a href="dashboard.html">🏠 Dashboard</a><a class="active" href="subjects.html">📚 Subjects</a><a href="enrollment.html">📝 My Enrollment</a><a href="profile.html">👤 My Profile</a></nav><button class="logout-btn" onclick="logout()">↪ Logout</button></aside><main class="main-content"><header class="topbar"><button class="menu-toggle" onclick="document.getElementById('sidebar').classList.toggle('open')">☰</button><div class="top-title"><strong>${title}</strong><small>University Student Portal</small></div><div class="user-mini"><span>${u.name}</span><div class="avatar">${initials(u)}</div></div></header><section class="page-content">${content}</section></main></div>`}